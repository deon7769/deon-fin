import { User } from "lucide-react";
import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function PerfilPage() {
  return <PlaceholderPage title="Perfil" icon={User} />;
}
