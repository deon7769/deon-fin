# Production defaults to same-origin /api.
# Use this override only for local Next dev server against FastAPI on :8000.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000/api

# Session login is still a cutover flag. Keep false until PostgreSQL auth is ready.
NEXT_PUBLIC_AUTH_ENABLED=false
